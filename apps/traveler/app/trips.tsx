import { NotBuilt } from '../src/NotBuilt';

/** Trips — Board 07 was never run through Claude Design (HANDOVER.md §13). */
export default function TripsScreen() {
  return <NotBuilt illo="dhow" />;
}
