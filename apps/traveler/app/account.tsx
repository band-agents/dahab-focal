import { NotBuilt } from '../src/NotBuilt';

/** Account — Board 08 was never run through Claude Design (HANDOVER.md §13). */
export default function AccountScreen() {
  return <NotBuilt illo="camel" />;
}
